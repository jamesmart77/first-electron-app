{
  "name": "projects",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "start-watch": "nodemon --exec npm start",
    "package-macOS": "electron-packager . Electron4Life --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "package-win": "electron-packager . Electron4Life --platform=win32 --arch=ia32 --out=release-builds",
    "package-linux": "electron-packager . Electron4Life --platform=linux --arch=x64 --prune=true --out=release-builds"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.6",
    "electron-packager": "^14.0.1",
    "nodemon": "^1.19.1"
  },
  "dependencies": {
    "node-notifier": "^8.0.1"
  }
}
